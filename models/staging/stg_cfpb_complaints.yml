version: 2

models:
  - name: stg_cfpb_complaints
    columns:
      - name: PRODUCT
        tests: 
          - not_null
      - name: COMPLAINT_ID
        tests:
          - not_null
          - unique
      - name: SUBMITTED_VIA
        tests:
          - accepted_values:
              values: ['Phone', 'Web', 'Referral', 'Postal mail', 'Mail']
          
      